import React from 'react';
import TurnDisplay from '../containers/TurnDisplay';

const Spectator = () => {
    return (
        <div>
            <TurnDisplay />
        </div>
    );
};

export default Spectator;