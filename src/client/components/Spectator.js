import React from 'react';
import RoomStateJson from './RoomStateJson.js';

const Spectator = () => {
    return (
        <div>
            <h1>Spectator</h1>
            <RoomStateJson />
        </div>
    );
};

export default Spectator;
